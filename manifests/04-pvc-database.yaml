apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-database
  namespace: ns-deploy
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  volumeName: pv-database
